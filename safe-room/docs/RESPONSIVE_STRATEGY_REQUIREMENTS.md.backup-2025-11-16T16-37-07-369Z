---
title: RESPONSIVE STRATEGY REQUIREMENTS
version: v1.0.0
last_updated: 2025-11-16
status: active
category: technical
---
# 响应式策略需求文档（Responsive Strategy v1.0）

> 版本：v1.0
> 更新日期：2025-11-16
> 适用范围：`springboot1ngh61a2/src/main/resources/front/front`

---

## 1. 文档目的

制定全站响应式设计策略，确保在 PC（≥1200px）、Pad（768-1199px）、Mobile（<768px）三个终端上提供一致、优质的用户体验。

---

## 2. 设计原则

### 2.1 核心理念

| 原则 | 说明 |
| --- | --- |
| **内容优先** | PC 展示完整信息，Pad/Mobile 按重要性分层展示 |
| **交互一致** | 核心操作在三端保持相同逻辑，适配终端特性 |
| **性能平衡** | PC 全动效，Pad 简化，Mobile 静态化 |
| **可访问性** | 支持键盘导航、屏幕阅读器、触控操作 |

### 2.2 断点定义

| 终端 | 断点范围 | 容器宽度 | 列数 | 网格间距 |
| --- | --- | --- | --- | --- |
| PC | ≥1200px | 1200px | 12列 | 24px |
| Pad | 768-1199px | 100% - 48px | 8列 | 16px |
| Mobile | <768px | 100% - 32px | 4列 | 12px |

---

## 3. 组件响应式策略

### 3.1 布局组件

#### 3.1.1 ModuleListPage（列表页）

| 终端 | 布局策略 | 筛选面板 | 分页组件 |
| --- | --- | --- | --- |
| PC | 侧边栏筛选 + 主内容区 | 固定侧边栏，展开/收起 | 完整分页 + 页尺寸选择 |
| Pad | 顶部筛选抽屉 + 主内容区 | 抽屉式筛选面板 | 简化分页，隐藏页尺寸 |
| Mobile | 堆叠布局 + 底部筛选 | 底部弹窗筛选 | 无限滚动替代分页 |

#### 3.1.2 ModuleDetailPage（详情页）

| 终端 | 布局策略 | 侧边栏 | 底部操作 |
| --- | --- | --- | --- |
| PC | 双栏布局（内容/操作） | 固定右侧操作栏 | 页面底部 CTA |
| Pad | 单栏布局，操作内联 | 隐藏侧边栏，合并到内容流 | 粘性底部操作栏 |
| Mobile | 堆叠布局 | 折叠为手风琴 | 浮动操作按钮 |

#### 3.1.3 ModuleFormPage（表单页）

| 终端 | 表单布局 | 字段排列 | 提交按钮 |
| --- | --- | --- | --- |
| PC | 多列表单 + 分组 | 标签左对齐，字段横排 | 底部固定按钮组 |
| Pad | 双列表单 | 标签上对齐，字段堆叠 | 底部粘性按钮 |
| Mobile | 单列表单 | 标签上对齐，字段全宽 | 底部安全区域按钮 |

### 3.2 交互组件

#### 3.2.1 卡片组件（TechCard）

| 终端 | 显示密度 | Hover 效果 | 点击区域 |
| --- | --- | --- | --- |
| PC | 高密度，多列展示 | 上浮 + 阴影增强 | 整卡可点击 |
| Pad | 中等密度，双列 | 轻微上浮 | 主要区域可点击 |
| Mobile | 低密度，单列 | 无上浮，点击反馈 | 安全点击区域 |

#### 3.2.2 筛选组件（Filters）

| 终端 | 展开方式 | 重置/应用 | 高级筛选 |
| --- | --- | --- | --- |
| PC | 内联展开 | 按钮组 | 复选框 + 滑块 |
| Pad | 抽屉面板 | 底部按钮 | 标签选择 |
| Mobile | 底部弹窗 | 弹窗按钮 | 简化选项 |

#### 3.2.3 表格组件（DataTable）

| 终端 | 显示模式 | 排序筛选 | 操作列 |
| --- | --- | --- | --- |
| PC | 完整表格 | 表头排序 + 筛选 | 行内操作按钮 |
| Pad | 卡片模式 | 顶部筛选器 | 下拉菜单操作 |
| Mobile | 列表模式 | 筛选弹窗 | 滑动操作 |

---

## 4. 页面级响应式策略

### 4.1 首页（Home）

| 终端 | Hero 区域 | 服务卡片 | 底部内容 |
| --- | --- | --- | --- |
| PC | 全宽视频背景 | 6列网格 | 完整展示 |
| Pad | 缩放视频 | 4列网格 | 折叠部分内容 |
| Mobile | 静态图片 | 2列网格 | 关键内容优先 |

### 4.2 课程列表（Courses List）

| 终端 | 筛选面板 | 课程卡片 | 排序分页 |
| --- | --- | --- | --- |
| PC | 左侧固定 | 3列瀑布流 | 完整控件 |
| Pad | 顶部抽屉 | 2列瀑布流 | 简化分页 |
| Mobile | 底部弹窗 | 单列堆叠 | 无限滚动 |

### 4.3 课程详情（Course Detail）

| 终端 | 主要内容 | 预约区域 | 相关推荐 |
| --- | --- | --- | --- |
| PC | 双栏布局 | 右侧固定 | 底部网格 |
| Pad | 单栏流式 | 内联插入 | 折叠展示 |
| Mobile | 堆叠布局 | 底部粘性 | 横滑推荐 |

### 4.4 个人中心（User Center）

| 终端 | 数据面板 | 快捷入口 | 表单区域 |
| --- | --- | --- | --- |
| PC | 多列仪表盘 | 网格布局 | 右侧表单 |
| Pad | 双列布局 | 列表布局 | 折叠表单 |
| Mobile | 单列堆叠 | 宫格布局 | 全屏表单 |

### 4.5 支付页面（Payment）

| 终端 | 订单摘要 | 支付方式 | 状态反馈 |
| --- | --- | --- | --- |
| PC | 左侧摘要 | 双列方式 | 右侧状态 |
| Pad | 顶部摘要 | 单列方式 | 底部状态 |
| Mobile | 顶部摘要 | 底部抽屉 | 全屏状态 |

---

## 5. 动效响应式策略

### 5.1 性能分级

| 终端 | 动效复杂度 | 帧率要求 | 降级策略 |
| --- | --- | --- | --- |
| PC | 高复杂度 | ≥60fps | GPU 加速 |
| Pad | 中复杂度 | ≥45fps | 减少粒子 |
| Mobile | 低复杂度 | ≥30fps | 静态替代 |

### 5.2 动效映射

| 动效类型 | PC | Pad | Mobile |
| --- | --- | --- | --- |
| 页面过渡 | GSAP Timeline | 简化过渡 | 淡入淡出 |
| Hover 效果 | 上浮 + 发光 | 轻微上浮 | 点击反馈 |
| 加载状态 | 粒子动画 | 环形进度 | 骨架屏 |
| 成功反馈 | Lottie 动画 | 简化动画 | 静态图标 |

---

## 6. 技术实现

### 6.1 CSS 策略

```scss
// 响应式断点 mixin
@mixin responsive($breakpoint) {
  @if $breakpoint == mobile {
    @media (max-width: 767px) { @content; }
  } @else if $breakpoint == pad {
    @media (min-width: 768px) and (max-width: 1199px) { @content; }
  } @else if $breakpoint == pc {
    @media (min-width: 1200px) { @content; }
  }
}

// 使用示例
.component {
  // 默认样式 (PC)
  display: grid;
  grid-template-columns: repeat(3, 1fr);

  @include responsive(pad) {
    grid-template-columns: repeat(2, 1fr);
  }

  @include responsive(mobile) {
    display: block;
  }
}
```

### 6.2 JavaScript 策略

```typescript
// 响应式工具函数
export const useResponsive = () => {
  const isMobile = computed(() => window.innerWidth < 768);
  const isPad = computed(() => window.innerWidth >= 768 && window.innerWidth < 1200);
  const isPC = computed(() => window.innerWidth >= 1200);

  return { isMobile, isPad, isPC };
};

// 组件中使用
const { isMobile } = useResponsive();

// 条件渲染
<template>
  <MobileLayout v-if="isMobile" />
  <DesktopLayout v-else />
</template>
```

### 6.3 组合式 API

```typescript
// 响应式布局组合式函数
export const useResponsiveLayout = () => {
  const layout = ref<'grid' | 'list' | 'card'>('grid');

  const updateLayout = () => {
    if (window.innerWidth < 768) {
      layout.value = 'list';
    } else if (window.innerWidth < 1200) {
      layout.value = 'card';
    } else {
      layout.value = 'grid';
    }
  };

  onMounted(() => {
    updateLayout();
    window.addEventListener('resize', updateLayout);
  });

  onUnmounted(() => {
    window.removeEventListener('resize', updateLayout);
  });

  return { layout };
};
```

---

## 7. 可访问性要求

### 7.1 键盘导航

- Tab 键顺序：筛选控件 → 主要内容 → 操作按钮
- Enter/Space：激活按钮和链接
- 箭头键：导航选项卡和菜单

### 7.2 屏幕阅读器

- ARIA 标签：为动态内容提供 `aria-label`
- 状态公告：筛选结果变化时提示用户
- 语义化：正确使用 heading 层级

### 7.3 触控优化

- 点击区域：最小 44×44px
- 滑动操作：支持左右滑动切换内容
- 手势反馈：触控时提供视觉反馈

---

## 8. 测试验收标准

### 8.1 功能验收

| 终端 | 布局完整性 | 交互可用性 | 内容完整性 |
| --- | --- | --- | --- |
| PC | ✅ 完整展示所有功能 | ✅ 鼠标/键盘操作正常 | ✅ 显示全部信息 |
| Pad | ✅ 布局适应屏幕 | ✅ 触控操作流畅 | ✅ 核心信息完整 |
| Mobile | ✅ 单列布局清晰 | ✅ 触控友好 | ✅ 关键信息优先 |

### 8.2 性能验收

| 终端 | 首屏加载 | 滚动性能 | 内存使用 |
| --- | --- | --- | --- |
| PC | ≤ 2s | ≥ 60fps | ≤ 100MB |
| Pad | ≤ 3s | ≥ 45fps | ≤ 80MB |
| Mobile | ≤ 4s | ≥ 30fps | ≤ 60MB |

---

## 9. 实施路线图

### 9.1 第一阶段：基础响应式（当前）

- ✅ 断点系统建立
- ✅ 核心组件适配
- ✅ 页面布局框架

### 9.2 第二阶段：交互优化（进行中）

- 🔄 动效性能优化
- 🔄 交互一致性检查
- 🔄 可访问性增强

### 9.3 第三阶段：体验提升（计划中）

- 📋 终端特性定制
- 📋 性能监控体系
- 📋 用户体验数据分析

---

## 10. 维护与更新

### 10.1 定期审查

- 每季度检查新页面响应式实现
- 根据用户反馈调整断点和布局
- 监控性能指标，优化动效复杂度

### 10.2 文档更新

- 新组件开发时必须包含响应式策略
- 设计系统更新时同步响应式指南
- 用户研究发现问题时及时调整策略

---

> 本文档为全站响应式设计的指导性文件，所有新功能开发必须遵循此策略。如遇特殊需求，可在设计评审时提出调整方案。</contents>
</xai:function_call">...
