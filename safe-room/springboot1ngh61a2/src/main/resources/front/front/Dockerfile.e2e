# Frontend E2E测试 Dockerfile
FROM mcr.microsoft.com/playwright:v1.56.1-jammy

WORKDIR /app

# 安装Node.js依赖
COPY package*.json ./
RUN npm ci

# 复制源代码
COPY . .

# 安装Playwright浏览器（如果需要）
RUN npx playwright install --with-deps chromium

# 设置环境变量
ENV CI=true
ENV E2E_NO_SERVER=true
ENV E2E_BASE_URL=${E2E_BASE_URL:-http://host.docker.internal:8082}
ENV BACKEND_URL=${BACKEND_URL:-http://host.docker.internal:8080}

# 默认运行所有e2e测试
CMD ["npm", "run", "test:e2e"]

