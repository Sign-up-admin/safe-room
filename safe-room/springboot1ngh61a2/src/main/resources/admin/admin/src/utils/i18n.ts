// translate router.meta.title, be used in breadcrumb sidebar tagsview
export function generateTitle(title: string): string {
  // Simplified version - can be extended with i18n later
  return title
}
