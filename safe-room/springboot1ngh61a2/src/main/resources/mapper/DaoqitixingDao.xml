<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dao.DaoqitixingDao">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.entity.DaoqitixingEntity" id="daoqitixingMap">
        <result property="yonghuzhanghao" column="yonghuzhanghao"/>
        <result property="yonghuxingming" column="yonghuxingming"/>
        <result property="touxiang" column="touxiang"/>
        <result property="huiyuankahao" column="huiyuankahao"/>
        <result property="youxiaoqizhi" column="youxiaoqizhi"/>
        <result property="tixingshijian" column="tixingshijian"/>
        <result property="beizhu" column="beizhu"/>
    </resultMap>

	<select id="selectListVO"
		resultType="com.entity.vo.DaoqitixingVO" >
		SELECT * FROM daoqitixing  daoqitixing         
        <where>
          ${ew.sqlSegment}
        </where>
	</select>
	
	<select id="selectVO"
		resultType="com.entity.vo.DaoqitixingVO" >
		SELECT  daoqitixing.* FROM daoqitixing  daoqitixing 	
 		<where>
          ${ew.sqlSegment}
        </where>
	</select>

    <select id="selectListView"
		resultType="com.entity.view.DaoqitixingView" >

		SELECT  daoqitixing.* FROM daoqitixing  daoqitixing 	        
        <if test="ew != null and ew.sqlSegment != null and ew.sqlSegment.trim() != ''">
            <choose>
                <when test="ew.sqlSegment.trim().toUpperCase().startsWith('ORDER') or ew.sqlSegment.trim().toUpperCase().startsWith('GROUP') or ew.sqlSegment.trim().toUpperCase().startsWith('HAVING') or ew.sqlSegment.trim().toUpperCase().startsWith('LIMIT')">
                    ${ew.sqlSegment}
                </when>
                <otherwise>
                    <where>
                        ${ew.sqlSegment}
                    </where>
                </otherwise>
            </choose>
        </if>
	</select>
	
	<select id="selectView"
		resultType="com.entity.view.DaoqitixingView" >
		SELECT * FROM daoqitixing  daoqitixing
		<where>
          ${ew.sqlSegment}
        </where>
	</select>
	


</mapper>
